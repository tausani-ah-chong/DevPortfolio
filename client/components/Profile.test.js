// import React from 'react'
// import { setStore } from './profileHelper'
// import { renderWithRedux } from '../test-utils'
// import Profile from './Profile'
// import consume from '../consume'

// jest.mock('../consume')
// jest.mock('./profileHelper')
// jest.mock('./Profile')
// jest.mock('../store')

// afterEach(() => {
//   return jest.resetAllMocks()
// })

describe('Profile', () => {
  it('Show Dev information', () => {
    // consume.mockImplementation(() => {
    //   return Promise.resolve({
    //     devs: [
    //       {
    //         id: 1,
    //         name: 'Tausani',
    //         bio: 'I dont like htis',
    //         languages: [{ id: 1, name: 'C#' }],
    //         projects: [{ id: 1, name: 'Cool project' }],
    //         platforms: [{ id: 1, name: 'Web' }]
    //       }
    //     ]
    //   })
    // })
    // renderWithRedux(<Profile />)
    // return consume()
    //   .then(res => {
    //     expect(res.devs).toHaveLength(2)
    //     expect(res.devs[0].bio).toMatch('I dont')
    return undefined
    //       })
  })
})

// TODO:
// Fix Profile test
